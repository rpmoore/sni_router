# Lookup

* [lookup-contract](lookup-contract.md) - The `RouteLookup` trait: what implementations receive,
  what they return, and the rules that keep matching policy in the library.
* [cached-lookup](cached-lookup.md) - The `CachedLookup` decorator: TTLs, single-flight loads,
  stale-if-error, invalidation, and its consistency contract.
